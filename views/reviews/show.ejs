<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <%- include('../partials/header.ejs') %>
        <h2>
            <%= review.weekNumber %>
        </h2>
        <ul class="displayinfo">
            <p class="textHighlight"> rating: <%= review.rating %></p>
            <p class="textHighlight">why: <%= review.weeklyReview %></p>
            <p class="textHighlight">Date: <%= review.createdAt.toLocaleDateString() %></p>
            <a href="/student/reviews/<%=review._id%>/edit"><button>edit Review</button></a>
            <form action="/student/reviews/<%= review._id%>?_method=DELETE %>" method="POST">
                <button>delete Review</button>
            </form>
        </ul>